name: PR Checks

on:
  pull_request:
    branches: [main, master]

env:
  PYTHON_VERSION: "3.10"

jobs:
  unit-tests:
    name: Unit Tests + Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: 'backend/requirements*.txt'

      - name: Install dependencies
        working-directory: backend
        run: pip install -r requirements.txt -r requirements-test.txt

      - name: Run unit tests with coverage
        working-directory: backend
        run: |
          python -m pytest tests/ --ignore=tests/integration \
            --cov=app --cov-report=term-missing --cov-fail-under=70 -v

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: 'backend/requirements*.txt'

      - name: Install ruff
        run: pip install "ruff>=0.8.0"

      - name: Ruff check
        working-directory: backend
        run: ruff check app/ tests/

      - name: Ruff format check
        working-directory: backend
        run: ruff format --check app/ tests/

  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    # Back to 20 (#418). #417's DEQM tests had roughly doubled this job —
    # 7m33s on #411 before it landed, 20m15s on the same PR after — and the
    # ceiling was temporarily raised to 35 so PRs could merge at all. The cause
    # was that every job in test_deqm_submit_data_workflow.py ran unscoped and
    # so evaluated the whole 319-patient CDR against a single measure; those
    # jobs are now scoped to each measure's own Group. Measured locally on that
    # file: 24m36s -> 11m23s, with test execution alone 1188s -> 383s.
    # The file is still NOT in the --ignore list below, deliberately: that is
    # what stops DEQM regressions reaching main unnoticed.
    timeout-minutes: 20
    env:
      # Soft default: STRICT_STU6=0 for rollout week. Flip to 1 once all connectathon measures pass in CI.
      STRICT_STU6: "0"
      USE_PREBAKED: "1"
      REQUIRE_PREBAKED: "1"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: |
            backend/requirements.txt
            backend/requirements-test.txt

      - name: Install dependencies
        working-directory: backend
        run: pip install -r requirements.txt -r requirements-test.txt

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Run integration tests
        run: |
          ./scripts/run-integration-tests.sh \
            --ignore=tests/integration/test_golden_measures.py \
            --ignore=tests/integration/test_connectathon_measures.py \
            --ignore=tests/integration/test_full_workflow.py \
            --ignore=tests/integration/test_groups_dropdown.py \
            --ignore=tests/integration/test_full_jobs_pipeline.py \
            --ignore=tests/integration/test_factory_reset.py \
            --durations=25 \
            2>&1 | tee /tmp/test-output.txt
          STATUS=${PIPESTATUS[0]}
          echo "## Integration Test Timings" >> "$GITHUB_STEP_SUMMARY"
          grep -A 200 'slowest durations' /tmp/test-output.txt >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit $STATUS

  script-security-lint:
    name: Script Security Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Reject set -x in prod scripts
        run: |
          # set -x traces every command to stderr; any in-scope secret variable
          # gets written to the systemd journal and CI logs.
          if grep -rn 'set -x' scripts/ --include='*.sh' \
              | grep -v 'scripts/tests/' | grep -v '#.*set -x'; then
            echo "ERROR: set -x found in scripts/ — remove it (leaks secrets to logs)"
            exit 1
          fi
          echo "OK: no set -x in scripts/"

      - name: Reject variable expansion of PASSWORD in echo/printf
        run: |
          # Match only when the $PASSWORD variable itself is being expanded —
          # not when the word "PASSWORD" appears in a literal log message.
          if grep -rn -E '(echo|printf)[^#]*\$\{?POSTGRES_PASSWORD' scripts/ --include='*.sh' \
              | grep -v 'scripts/tests/'; then
            echo "ERROR: \$POSTGRES_PASSWORD expanded in echo/printf in scripts/ — redact before logging"
            exit 1
          fi
          echo "OK: no \$POSTGRES_PASSWORD expansion in echo/printf in scripts/"

      - name: Bash unit tests
        run: bash scripts/tests/run_tests.sh

  frontend-build:
    name: Frontend Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v6
        with:
          node-version: "20"

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Test
        working-directory: frontend
        run: npm test -- --watchAll=false
        env:
          CI: true

      - name: Build
        working-directory: frontend
        run: npm run build

  config-validation:
    name: Config Validation
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Validate Caddyfile
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/Caddyfile:/etc/caddy/Caddyfile \
            -e CADDY_HOST=lenny.bellese.dev \
            caddy:2-alpine caddy validate --config /etc/caddy/Caddyfile

      - name: Validate docker-compose.prod.yml
        env:
          CADDY_HOST: lenny.bellese.dev
        run: docker compose -f docker-compose.yml -f docker-compose.prod.yml config --quiet

      - name: Validate IAM policy JSON
        run: jq . iam/leonard-ec2-prod-cloudwatch-policy.json > /dev/null
